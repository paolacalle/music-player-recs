
# Nov 7 
- started the react app `npm create vite@latest my-react-app -- --template react`


# Nov 11
- Next steps should be rendering the preview spotify -- if no preview from sportify render 
from YT api 